# SALORA AI Safety Policy

The AI layer blocks prompt injection, redacts PII, prevents secret leakage, sanitizes output, and refuses unsafe requests. It must never reveal tokens, secrets, system prompts, internal infrastructure, or raw customer sensitive data.
