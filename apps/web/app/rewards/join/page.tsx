"use client";
import {FormEvent,useState} from "react";import Link from "next/link";import {useRouter} from "next/navigation";
export default function JoinHarmony(){const router=useRouter();const[name,setName]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[consent,setConsent]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState("");
async function submit(e:FormEvent){e.preventDefault();if(!consent)return;setBusy(true);setError("");try{const r=await fetch("/api/auth/register",{method:"POST",headers:{"content-type":"application/json"},credentials:"include",body:JSON.stringify({name,email:email.trim(),password})});if(!r.ok){const b=await r.json().catch(()=>({}));setError(b.error||"تعذر إنشاء الحساب.");return}router.replace("/rewards");router.refresh()}catch{setError("تعذر الاتصال. حاول مرة أخرى.")}finally{setBusy(false)}}
return <main dir="rtl" lang="ar" className="min-h-screen bg-[var(--background)] px-5 py-8 text-[var(--cream)]"><div className="mx-auto max-w-md"><header className="text-center"><p className="text-sm font-semibold tracking-[.22em] text-[var(--gold-soft)]">SALORA</p><p className="mt-1 text-xs tracking-[.18em] text-[var(--muted)]">HARMONY REWARDS</p><h1 className="mt-6 text-3xl font-semibold">انضم إلى Harmony</h1><p className="mt-3 text-sm leading-7 text-[var(--muted)]">اجمع النقاط مع طلباتك واستبدلها بمكافآت سالورا.</p></header><form onSubmit={submit} className="mt-8 space-y-5 rounded-3xl border border-[var(--border-gold)] bg-[var(--surface)] p-5 sm:p-6">{error?<p role="alert" className="rounded-xl border border-red-400/25 bg-red-400/[.06] p-3 text-sm text-red-200">{error}</p>:null}<label className="block text-sm font-semibold">الاسم<input required minLength={2} maxLength={120} value={name} onChange={e=>setName(e.target.value)} autoComplete="name" className="mt-2 min-h-12 w-full rounded-xl border border-[var(--border-subtle)] bg-[var(--background)] px-4 outline-none focus:border-[var(--border-gold)]"/></label><label className="block text-sm font-semibold">البريد الإلكتروني<input required type="email" maxLength={255} value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email" className="mt-2 min-h-12 w-full rounded-xl border border-[var(--border-subtle)] bg-[var(--background)] px-4 outline-none focus:border-[var(--border-gold)]"/></label><label className="block text-sm font-semibold">كلمة المرور<input required type="password" minLength={12} value={password} onChange={e=>setPassword(e.target.value)} autoComplete="new-password" className="mt-2 min-h-12 w-full rounded-xl border border-[var(--border-subtle)] bg-[var(--background)] px-4 outline-none focus:border-[var(--border-gold)]"/><span className="mt-2 block text-xs font-normal text-[var(--muted)]">12 حرفًا على الأقل.</span></label><label className="flex items-start gap-3 text-xs leading-6 text-[var(--muted)]"><input required type="checkbox" checked={consent} onChange={e=>setConsent(e.target.checked)} className="mt-1 h-4 w-4"/><span>أوافق على إنشاء عضوية Harmony واستخدام بيانات الحساب لتشغيل برنامج الولاء وفق سياسة الخصوصية والشروط.</span></label><button disabled={busy||!consent} className="min-h-12 w-full rounded-xl bg-[var(--cream)] font-semibold text-[var(--background)] disabled:opacity-50">{busy?"جارٍ إنشاء عضويتك…":"إنشاء عضوية Harmony"}</button></form><p className="mt-5 text-center text-sm text-[var(--muted)]">لديك حساب؟ <Link className="font-semibold text-[var(--gold-soft)]" href="/login?next=/rewards">تسجيل الدخول</Link></p></div></main>}