# SALORA Redis Runtime

Redis uses `ioredis` with lazy connection, bounded retry strategy, reconnect metrics, health checks, and graceful shutdown.

Prepared usage:

- request caching
- runtime caching
- future session support
- BullMQ connection backbone
